# Core dependencies
llama-cpp-python>=0.2.0
python-dotenv>=1.0.0
tqdm>=4.65.0

# Optional: uncomment if you need better performance
# llama-cpp-python[server]  # includes uvicorn for advanced features

# Note: tkinter comes with Python standard library
# If tkinter is missing on Linux, install: sudo apt-get install python3-tk
