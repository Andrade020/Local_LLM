# Core dependencies
python-dotenv>=1.0.0
psutil>=5.9.0

# A inferência roda no llama-server.exe do llama.cpp (baixado à parte, ver README):
# https://github.com/ggml-org/llama.cpp/releases

# Note: tkinter comes with Python standard library
# If tkinter is missing on Linux, install: sudo apt-get install python3-tk
